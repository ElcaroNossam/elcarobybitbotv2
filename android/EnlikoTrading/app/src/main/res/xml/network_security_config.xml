<?xml version="1.0" encoding="utf-8"?>
<!--
    Network Security Configuration for Enliko Trading App
    
    SECURITY: TLS only, no cleartext traffic
    Certificate pinning DISABLED for compatibility
-->
<network-security-config>
    
    <!-- Base config: no cleartext allowed, use system certificates -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    
    <!-- Debug config: allow user certificates for debugging -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="user" />
            <certificates src="system" />
        </trust-anchors>
    </debug-overrides>
    
    <!-- Production domain: enliko.com - NO PINNING for Cloudflare compatibility -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">enliko.com</domain>
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
        <!-- Certificate pinning disabled - Cloudflare rotates certificates frequently -->
    </domain-config>
    
    <!-- Allow cleartext only for localhost in debug (Android emulator) -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
    
</network-security-config>
