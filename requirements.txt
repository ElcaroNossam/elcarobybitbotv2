# Elcaro Bybit Trading Bot v2 - Dependencies
# Python 3.10+ required
# Last updated: December 23, 2025

# =====================================================
# CORE TELEGRAM & ASYNC
# =====================================================
python-telegram-bot>=22.0
aiohttp>=3.12.0
python-dotenv>=1.0.0

# =====================================================
# FEATURES & UTILITIES
# =====================================================
feedparser>=6.0.10
reportlab>=4.0.0
pillow>=10.0.0

# =====================================================
# BLOCKCHAIN & WEB3
# =====================================================
# TON blockchain for ELCARO token payments
pytoniq>=0.1.35
pytoniq-core>=0.1.35
tonsdk>=1.0.15

# Ethereum/EVM for cold wallet trading (MetaMask)
eth-account>=0.10.0
web3>=6.11.0

# =====================================================
# HTTP & API CLIENTS
# =====================================================
httpx>=0.25.0
requests>=2.31.0

# =====================================================
# DATA & CRYPTO
# =====================================================
cryptography>=41.0.0
numpy>=1.24.0  # Required for advanced indicators, Monte Carlo, walk-forward optimization

# =====================================================
# DATABASE & ORM
# =====================================================
# SQLite is built-in, no external deps needed
# PostgreSQL for scalable architecture (10K+ users)
asyncpg>=0.29.0
psycopg2-binary>=2.9.9

# =====================================================
# CACHING & DATA STRUCTURES
# =====================================================
cachetools>=5.3.0  # TTLCache, LRUCache for memory-safe caching

# =====================================================
# DISTRIBUTED CACHE & MESSAGE BROKER
# =====================================================
redis>=5.0.0
hiredis>=2.3.0  # C parser for faster Redis

# =====================================================
# TASK QUEUE (Celery)
# =====================================================
celery>=5.3.0
flower>=2.0.0  # Celery monitoring UI

# =====================================================
# WEBAPP (FastAPI + WebSockets)
# =====================================================
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
python-multipart>=0.0.6
jinja2>=3.1.2
websockets>=12.0

# =====================================================
# AUTHENTICATION & SECURITY
# =====================================================
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
PyJWT>=2.8.0

# =====================================================
# TESTING & QUALITY
# =====================================================
pytest>=9.0.0
pytest-asyncio>=1.3.0
pytest-cov>=4.1.0
pytest-mock>=3.11.0
pytest-anyio>=4.9.0

# =====================================================
# EXCHANGE INTEGRATIONS
# =====================================================
# Bybit - via pybit (if needed, or direct API via aiohttp)
# HyperLiquid - custom implementation in hyperliquid/ and hl_adapter.py

# =====================================================
# OPTIONAL: MONITORING & METRICS
# =====================================================
# prometheus-client>=0.19.0  # Uncomment if using Prometheus

# =====================================================
# NOTES
# =====================================================
# - All deps use >= to allow minor updates
# - For production, pin exact versions in requirements-lock.txt
# - Unified architecture requires no additional deps (uses existing aiohttp)
# - Translations require no deps (pure Python dicts)
# - Screener uses FastAPI WebSocket (included in fastapi+uvicorn)
# - Real-time market data via Binance API (no additional deps)
# - CSS design system: pure CSS, no preprocessors needed

# =====================================================
# RECENT UPDATES (December 23, 2025)
# =====================================================
# - ✅ Screener WebSocket API with real-time updates
# - ✅ Unified models (Position, Balance, OrderResult)
# - ✅ Multi-exchange support (Bybit + HyperLiquid)
# - ✅ Enhanced testing suite (102+ tests passing)
# - ✅ ElCaro design system CSS (gradient UI)# - ✅ Position calculator matching bot.py EXACT formula
# - ✅ 50+ advanced indicators (trend, momentum, volatility)
# - ✅ Orderbook analyzer with slippage modeling
# - ✅ Risk management (Kelly, Optimal F, Sharpe/Sortino)
# - ✅ Multi-timeframe analysis with confluence detection
# - ✅ Visual strategy builder with JSON export
# - ✅ Monte Carlo simulation & stress testing
# - ✅ Walk-forward optimization with genetic algorithm
# - ✅ Trading terminal with B/S hotkeys & position calculator